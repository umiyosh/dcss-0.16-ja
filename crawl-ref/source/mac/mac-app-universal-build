#!/bin/sh

# Kept as a compatibility entry point for older tooling. Releases are
# intentionally built for the host architecture: the supported download is
# Apple Silicon-only, and the bundled dependencies are built for that target.
exec make "$@"
